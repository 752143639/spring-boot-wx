<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wx.realware.grp.pt.pb.respority.mapper.fingerconfig.EmployeeMapper">
<!--配置型mybits-->
 <select id="getEmpByid" resultType="wx.realware.grp.pt.pb.bean.common.Employee">
      select * from employee where id =#{id}

 </select>

     <insert id="insertEmp" parameterType="wx.realware.grp.pt.pb.bean.common.Employee" useGeneratedKeys="true" keyProperty="id">
        insert into employee(lastName,email,gender,d_id) values (#{lastName},#{email},#{gender},#{d_id})
    </insert>

</mapper>